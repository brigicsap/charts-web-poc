export const notes = `
## Notes

This seems to be the easiest to style but fairly fussy with interactions. Eg. the hover/active states.

- **[bar]** bars totally not centred on hover. not to mention the click state - I could not get this working
- **[stacked bar]** no support for paddings between stacks, so needs css transform to fake it - the bar then overlaps the x-axis
`;
